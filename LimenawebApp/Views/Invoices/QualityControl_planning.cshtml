@model IEnumerable<LimenawebApp.Models.QualityControl_SO>

@{
    ViewBag.Title = "Distribuidora Limeña - Quality Control planning";
    Layout = "~/Views/Shared/_LayoutMain.cshtml";
}
<div class="page__header">
    <div class="container-fluid page__heading-container">
        <div class="page__heading d-flex align-items-center">
            <div class="flex">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb mb-0">
                        <li class="breadcrumb-item"><a href="#">Operations</a></li>
                        <li class="breadcrumb-item active" aria-current="page">Quality Control</li>
                    </ol>
                </nav>
                <h1 class="m-0">Quality Control</h1>
            </div>

        </div>
    </div>
</div> <!-- // END page__header -->
<!--Page Header menu-->
@Html.Partial("~/Views/Shared/New_design/Menus/Operations.cshtml")

<div class="container-fluid page__container">
    <div class="row">

        <div class="card col-md-12">
            <div class="card-header card-header-large bg-white">
                <div class="row">
                    <div class="col-md-4">
                        <div class="form-group">
                            <label class="col-sm-12 col-form-label ">Filter by Date</label>
                            <div id="reportrange" style="background: #fff; cursor: pointer; padding: 5px 10px;margin-left: 14px; margin-right:15px; border: 1px solid #ccc; width: 100%">
                                <i class="mdi mdi-calendar"></i>&nbsp;
                                <span></span> <i class="fa fa-caret-down"></i>
                            </div>

                        </div>


                    </div>
                    <div class="col-md-3"></div>  
                    <div class="col-md-2"></div>  
                    
                    <div class="col-md-3">
                  
                            <button type="button" class="btn btn-success btn-lg" data-toggle="modal" data-target="#modal-containers" style="font-size:15px !important;"><i></i>Add additional Container</button>
                        
                    </div>
                </div>

           
            </div>
            <div class="card-header card-header-tabs-basic nav" role="tablist">
                @if (ViewBag.company == "01")
    {
            <a href="#main" class="active" data-toggle="tab" role="tab" aria-controls="activity_all" aria-selected="true"> <i class="material-icons icon-muted icon-20pt mr-2">home</i> Nashville, TN</a>
            <a href="#second" data-toggle="tab" role="tab" aria-selected="false"><i class="material-icons icon-muted icon-20pt mr-2">local_shipping</i>Louisville, KY</a>
}
else if (ViewBag.company == "02")
{
            <a href="#second" class="active" data-toggle="tab" role="tab" aria-selected="false"> <i class="material-icons icon-muted icon-20pt mr-2">home</i> Louisville, KY</a>
}

            </div>
            <div class="card-body tab-content">
                @if (ViewBag.company == "01")
    {

            <div class="tab-pane active show fade" id="main">

                <div class="card-body pt-3 pb-4 ">
                    <div class="table-responsive">
                        <table id="data_tableEditor" class="table table-bordered " cellspacing="0">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>NAME</th>
                                    <th>DATE(DEPARTURE)</th>
                                    <th>DRIVER</th>
                                    <th>TRUCK</th>
                                    <th>ROUTE LEADER</th>
                                    <th>STATUS</th>
                                    <th>ACTIONS</th>


                                </tr>
                            </thead>
                            <tfoot>
                            </tfoot>
                            <tbody>
                                @foreach (var so in Model.Where(a => a.Warehouse == "01"))
                    {
                        <tr style="font-size:14px;">
                            <td style="font-size:12px !important;">@so.ID_Route</td>
                            <td>
                                @so.Route_name
                                @if (so.isfinished == true && so.Invoiced == true)
                    {<small class="text-success">(Closed)</small> }
        else if (so.isfinished == true && so.Invoiced == false)
        {<small class="text-warning">(In progress)</small>}

                                @if (so.freezers.Count > 0) {
                                   <br />
                                   foreach (var freezer in so.freezers) {
                                       <img src="~/Content/assets/images/marker/congelador.png" title="@freezer.name" style="width:25px;"/>
                                   }
                                   
                                }
                            </td>
                            <td>@so.Departure</td>
                            <td>@so.Driver_name</td>
                            <td>@so.Truck_name</td>
                            <td>@so.Routeleader_name</td>
                            <td>

                                <div class="progress-title">
                                    <span id="forms_p1" class="">@Convert.ToDecimal(so.query1).ToString("n2")% @so.query2 Items</span>
                                </div>
                                <div class="progress" style="height: 2px;">
                                    @if (Convert.ToDecimal(so.query1) < 100)
                        {
                            <div class="progress-bar bg-warning" role="progressbar" style="width: @so.query1%;" aria-valuemax="100"></div>
            }
            else
            {
                <div class="progress-bar bg-success" role="progressbar" style="width: @so.query1%;" aria-valuemax="100"></div>
}
                                </div>


                            </td>
                            <td><div class="button-list">
    <a href="@Url.Action("QualityControl", "Invoices", new { id = so.ID_Route })" class="btn  btn-info" style="font-size:12px !important;"><i class="fa fa-external-link"></i>Details</a>

    @if (so.isfinished == true && so.Invoiced == true)
    {
        <a href="#" onclick="PrintRoute(@so.ID_Route)" class="btn btn-warning " style="font-size:12px !important;"><i class="fa fa-print"></i>Print</a>
        <a href="#" onclick="PrintRouteTNWMS(@so.ID_Route)" class="btn btn-warning " style="font-size:12px !important;"><i class="fa fa-print"></i>Preview</a>
        <a href="#" onclick="PrintRouteTNWMSBarcode(@so.ID_Route)" class="btn btn-warning " style="font-size:12px !important;"><i class="fa fa-print"></i>Preview Barcode</a>
        <a href="@Url.Action("Print_DPCD", "Invoices", new { id_route = so.ID_Route })" target="_blank" class="btn btn-warning" style="font-size:12px !important;"><i class="fa fa-print"></i>Daily Payment CD</a>

        @*<a href="#" onclick="PrintDailyPayment(so.ID_Route)" class="btn btn-sm btn-outline-warning btn-sm form-pill"><i class="fa fa-print"></i>Daily Payment CD</a>*@

    }



    @if (Convert.ToDecimal(so.query1) >= 100)
    {
        if (so.query3 == 0)
        {
            <a href="#" class="btn btn-success" style="font-size:12px !important;" onclick="closeOrders('@so.ID_Route','@so.Route_name')"><i class="fa fa-close"></i>Close All Orders</a>
        }
        else
        {
            if (so.isfinished == false)

            {
                <a href="#" class="btn  btn-success" style="font-size:12px !important;" onclick="closeRoute(@so.ID_Route)"><i class="fa fa-close"></i>Invoice</a>

            }
            else
            {

            }


        }
    }
    else
    {


    }

</div></td>
                        </tr>
        }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <div class="tab-pane fade" id="second">
                <div class="card-body pt-3 pb-4">
                    @if (ViewBag.mostrarBOF > 0)
        {
            <a href="#" class="btn btn-sm btn-outline-success btn-sm form-pill" onclick="transferItems('@ViewBag.rutasids')"><i class="fa fa-close"></i>Close and Print Bill of Lading</a>
}

                    <div class="table-responsive">
                        <table id="data_tableEditor2" class="table table-bordered table-striped" cellspacing="0">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>NAME</th>
                                    <th>DATE(DEPARTURE)</th>
                                    <th>DRIVER</th>
                                    <th>TRUCK</th>
                                    <th>ROUTE LEADER</th>
                                    <th>STATUS</th>
                                    <th>ACTIONS</th>


                                </tr>
                            </thead>
                            <tfoot>
                            </tfoot>
                            <tbody>
                                @foreach (var so in Model.Where(a => a.Warehouse == "02" && a.query6 > 0))
                    {
                        <tr style="font-size:14px;">
                            <td style="font-size:12px;">@so.ID_Route</td>
                            <td>
                                @so.Route_name
                                @if (so.isfinished == true && so.Invoiced == true)
                    {<small class="text-success">(Closed)</small> }
        else if (so.isfinished == true && so.Invoiced == false)
        {<small class="text-warning">(In progress)</small>}

                                                                
                                @if (so.freezers.Count > 0) {
                                   <br />
                                   foreach (var freezer in so.freezers) {
                                       <img src="~/Content/assets/images/marker/congelador.png" title="@freezer.name" style="width:25px;"/>
                                   }
                                   
                                }
                            </td>
                            <td>@so.Departure</td>
                            <td>@so.Driver_name</td>
                            <td>@so.Truck_name</td>
                            <td>@so.Routeleader_name</td>
                            <td>
                                @if (so.transferred == 1)
                    {
                        <label class="badge text-warning">In Progress</label>
        }
        else if (so.transferred == 2)
        {
            <label class="badge text-success">Items were transferred</label>
}
else
{
            <div class="progress-title">
                <span id="forms_p1" class="">@Convert.ToDecimal(so.query4).ToString("n2")% @so.query5 Items</span>
            </div>
            <div class="progress" style="height: 2px;">
                @if (Convert.ToDecimal(so.query4) < 100)
    {
            <div class="progress-bar bg-warning" role="progressbar" style="width: @so.query4%;" aria-valuemax="100"></div>
}
else
{
            <div class="progress-bar bg-success" role="progressbar" style="width: @so.query4%;" aria-valuemax="100"></div>
}
            </div>

}



                            </td>
                            <td><div class="button-list">
                                @if (so.transferred == 1)
                    {

                    }
                    else if (so.transferred == 2)
                    {

                    }
                    else
                    {
                        <a href="@Url.Action("QualityControl", "Invoices", new { id = so.ID_Route })"  style="font-size:12px;" class="btn  btn-info "><i class="fa fa-external-link"></i>Details</a>
        }



                                @if (so.isfinished == true && so.Invoiced == true)
                    {
                    }


                                @if (Convert.ToDecimal(so.query4) >= 100)
                    {





                    }

                            </div></td>
                        </tr>
        }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
}
                @if (ViewBag.company == "02")
    {
            <div class="tab-pane active show fade" id="second">


                <div class="card-body pt-3 pb-4">
                    <div class="table-responsive">
                        <table id="data_tableEditor" class="table table-bordered" cellspacing="0">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>NAME</th>
                                    <th>DATE(DEPARTURE)</th>
                                    <th>DRIVER</th>
                                    <th>TRUCK</th>
                                    <th>ROUTE LEADER</th>
                                    <th>OTHER WHS</th>
                                    <th>STATUS</th>
                                    <th>ACTIONS</th>


                                </tr>
                            </thead>
                            <tfoot>
                            </tfoot>
                            <tbody>
                                @foreach (var so in Model)
                    {
                        <tr style="font-size:14px;">
                            <td style="font-size:12px;">@so.ID_Route</td>
                            <td>
                                @so.Route_name
                                @if (so.isfinished == true && so.Invoiced == true)
                    {<small class="text-success">(Closed)</small> }
        else if (so.isfinished == true && so.Invoiced == false)
        {<small class="text-warning">(In progress)</small>}

                                                                @if (so.freezers.Count > 0) {
                                   <br />
                                   foreach (var freezer in so.freezers) {
                                       <img src="~/Content/assets/images/marker/congelador.png" title="@freezer.name" style="width:25px;"/>
                                   }
                                   
                                }
                            </td>
                            <td>@so.Departure</td>
                            <td>@so.Driver_name</td>
                            <td>@so.Truck_name</td>
                            <td>@so.Routeleader_name</td>
                            <td>

                                <div class="progress-title">
                                    @if (ViewBag.existendeOtraBodega > 0)
                        {

                            <span id="forms_p1" class="">@Convert.ToDecimal(so.query4).ToString("n2")% @so.query5 Items</span>
            }
            else
            {
                <span id="forms_p1" class="">100%</span>
}
                                </div>
                                <div class="progress" style="height: 2px;">
                                    @if (Convert.ToDecimal(so.query4) < 100)
                        {
                            if (ViewBag.existendeOtraBodega > 0)
                            {

                                <div class="progress-bar bg-warning" role="progressbar" style="width: @so.query4%;" aria-valuemax="100"></div>
                }
                else
                {
                    <div class="progress-bar bg-success" role="progressbar" style="width:100%;" aria-valuemax="100"></div>
    }

}
else
{

            <div class="progress-bar bg-success" role="progressbar" style="width: @so.query4%;" aria-valuemax="100"></div>

}
                                </div>


                            </td>
                            <td>

                                <div class="progress-title">
                                    <span id="forms_p1" class="">@Convert.ToDecimal(so.query1).ToString("n2")% @so.query2 Items</span>
                                </div>
                                <div class="progress" style="height: 2px;">
                                    @if (Convert.ToDecimal(so.query1) < 100)
                        {
                            <div class="progress-bar bg-warning" role="progressbar" style="width: @so.query1%;" aria-valuemax="100"></div>
            }
            else
            {
                <div class="progress-bar bg-success" role="progressbar" style="width: @so.query1%;" aria-valuemax="100"></div>
}
                                </div>


                            </td>
                            <td>
                            <div class="button-list">
                                <a href="@Url.Action("QualityControl", "Invoices", new { id = so.ID_Route })" class="btn btn-info" style="font-size:12px;"><i class="fa fa-external-link"></i>View</a>
                                @if (ViewBag.existendeOtraBodega == 0)
                    {
                        <a href="@Url.Action("QualityControl_storagetype", "Invoices", new { id = so.ID_Route })" class="btn btn-info" style="font-size:12px;"><i class="fa fa-external-link"></i>Check Items</a>
        }
                                @if (so.isfinished == true && so.Invoiced == true)
                    {
                        <a href="#" onclick="PrintRouteWHS(@so.ID_Route)" class="btn  btn-warning " style="font-size:12px;"><i class="fa fa-print"></i>Print</a>
                        <a href="@Url.Action("Print_DPCD", "Invoices", new { id_route=so.ID_Route})" target="_blank" class="btn  btn-warning" style="font-size:12px;"><i class="fa fa-print"></i>Daily Payment CD</a>
        }


                                @if (Convert.ToDecimal(so.query1) >= 100 && so.query6 == 0)
                    {

                        if (so.query3 == 0)
                        {
                            <a href="#" class="btn  btn-success " style="font-size:12px;" onclick="closeOrders('@so.ID_Route','@so.Route_name')"><i class="fa fa-close"></i>Close All Orders</a>
            }
            else
            {
                if (so.isfinished == false)

                {
                    <a href="#" class="btn  btn-success" style="font-size:12px;" onclick="closeRoute(@so.ID_Route)"><i class="fa fa-close"></i>Invoice</a>

    }
    else
    {

    }


}
}
else
{


}
                            </div></td>
                        </tr>
        }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
}



            </div>
        </div>

    </div>

</div>
